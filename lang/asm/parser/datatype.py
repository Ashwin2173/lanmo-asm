from enum import Enum, auto

class DataType(Enum):
    INTEGER = auto()
    STRING = auto()
    IDENTIFIER = auto()
    FUNCTION = auto()
    NONE = auto()
    BOOLEAN = auto()
    BUILT_IN_FUNCTION = auto()
    FLOAT = auto()
