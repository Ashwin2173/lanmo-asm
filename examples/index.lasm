NATIVE print

main {
    PUSH 1
    PUSH 2
    PUSH 3
    MAKE_LIST 3
    STORE list

    LOAD list
    PUSH 1
    GET_INDEX

    LOAD list
    PUSH 100
    PUSH 1
    SET_INDEX
    STORE list

    PUSH print
        LOAD list
    CALL 1

    PUSH 0
    RETURN
}